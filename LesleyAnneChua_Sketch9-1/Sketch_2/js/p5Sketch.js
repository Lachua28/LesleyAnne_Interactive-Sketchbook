function setup() {
    createCanvas (600, 400);
    //accumulation
    background(74, 58, 53);
}

function draw() {
    noStroke();
    // direct manipulation
    ellipse(mouseX, mouseY, 40)
}